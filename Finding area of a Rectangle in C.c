#include<stdio.h>
int area(int length, int width)
{
	return length*width;
	// calculating area of a rectangle using function
}
int main(){
	int length=10;
	int width=4;
	// calling the above function
	int a=(length*width);
	printf("%d",a);
	return 0;
}
